name: example_flutter
description: Flutter example project for the buggy package.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ^3.41.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^9.0.0
